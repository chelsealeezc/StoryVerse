# StoryVerse 产品需求文档

- 版本：V2.0
- 日期：2026-07-24
- 状态：localhost 交互原型实施版
- 最新事实源：《功能详细说明（说到做到版）》及 2026-07-24 产品问答
- 第一阶段交付：中文 localhost 可交互预览

## Problem Statement

普通用户拥有关于家庭、城市、关系、成长、迁移、学校与工作等真实经历，但面对空白输入框时往往不知道如何开始，也不一定具备把记忆整理成完整叙事的能力。传统信息流倾向强化相似内容，公开评论区又容易把差异经验压缩成立场争论。

用户需要一个温和的叙事空间：先通过别人的简短回答降低表达压力，再通过结构化引导讲述一个真实的人生事件；AI 只负责整理标签和叙事结构，不能替用户改写故事或决定意义。完成表达后，用户既能遇见相似经历，也能主动进入与自己不同的城市、人生阶段和主题。

## Solution

StoryVerse 是一个桌面 Web 优先的生成式 AI 叙事组织与故事探索产品。第一阶段使用本地种子数据与模拟服务完成可交互闭环。

新用户先浏览 Icebreaker 星点，理解“每颗星来自一个真实的人”的社区氛围。Icebreaker 仅用于浏览和暖场，不计作用户的首个正式故事，也不解锁主页。

注册后，用户必须完成首个正式故事：选择人生事件引导、填写故事与上下文、查看模拟 AI 分析、修正标签并确认发布。发布后，用户分别在城市、人生阶段、主题三个维度选择“相似”或“相异”，系统返回目标 5 则故事及可理解的推荐理由。

用户至少打开 1 则推荐故事后即可进入轻量星图主页。主页以二维星点与故事卡片结合呈现，不实现复杂 3D 星空。用户可以搜索、筛选、修改共鸣偏好、查看喜欢记录和继续写故事。故事详情以内嵌层呈现，支持喜欢、不喜欢、关闭和举报；不设置公开评论区或产品内显式评分量表。

## Product Principles

1. AI 帮助整理、理解和连接，不替用户写作。
2. 用户对标题、标签和故事意义拥有最终确认权。
3. Icebreaker 是浏览型暖场，不是正式投稿。
4. 首个正式故事不可跳过，完成后才解锁主页。
5. 相似与相异推荐必须可解释，且“相异”不是随机。
6. 日常界面不提供公开评论区，不放置研究评分量表。
7. 喜欢记录属于主页能力，不另造“收藏”概念。
8. 城市由用户主动选择，只保留城市级标签，不获取精准定位。
9. 正文保存与 AI 分析分离，AI 失败不能导致故事丢失。
10. 行为研究只采集必要的聚合指标，不记录逐键内容或击键生物特征。

## Goals and First-Phase Success Criteria

1. 用户可以在 localhost 从暖场浏览走完注册、首故事输入、AI 分析、确认发布、共鸣选择、推荐浏览、轻量星图和故事反馈。
2. 页面刷新后，本地草稿、首故事门禁、共鸣偏好和反馈状态仍可恢复。
3. 推荐结果目标为 5 则；候选不足时允许减少并显示解释。
4. 至少打开 1 则推荐故事后，“进入故事星图”可用；不强制读完 5 则。
5. 喜欢与不喜欢互斥，均可撤销；喜欢记录在主页按时间倒序展示全部记录。
6. 举报必须选择原因，并在最终提交前二次确认。
7. 所有核心流程均使用中文；PC 1440px 为首要验证尺寸，并能降级到平板和移动宽度。

## User Stories

1. As a 首次访问者, I want to 浏览来自社区的 Icebreaker 星点, so that 我能先理解这里允许分享怎样的真实内容。
2. As a 首次访问者, I want to 看到社区规范的渐进式说明, so that 我知道故事不需要完美但需要真实。
3. As a 首次访问者, I want to 明白 Icebreaker 只是浏览体验, so that 我不会误以为已经完成首故事。
4. As a 新用户, I want to 创建本地演示账户, so that 我能进入完整原型流程。
5. As a 回访用户, I want to 使用已有演示账户登录, so that 我可以继续上次的进度。
6. As a 新用户, I want to 看到注册、首故事和进入星图的进度, so that 我理解为什么主页仍被锁定。
7. As a 新用户, I want to 看到匿名展示、城市级标签和可撤回说明, so that 我能判断是否愿意分享。
8. As a 未完成首故事的用户, I want to 被引导回尚未完成的步骤, so that 我不会丢失进度。
9. As a 已完成首故事的用户, I want to 登录后直接进入主页, so that 我能继续探索。
10. As a 用户, I want to 从人生转折、生命阶段、观念转变、重大经历中选择引导, so that 我不用面对空白输入框。
11. As a 用户, I want to 左右浏览人生事件引导卡片, so that 我能找到最接近自己经历的入口。
12. As a 用户, I want to 输入可选标题, so that 没有合适标题时也能先写正文。
13. As a 用户, I want to 输入或粘贴故事正文, so that 我能使用适合自己的表达方式。
14. As a 用户, I want to 看到 100–1500 字建议与实时字数, so that 我知道内容是否足以分析。
15. As a 用户, I want to 在内容偏短时收到建议而非强硬报错, so that 我不会被打断表达。
16. As a 用户, I want to 选择写完故事后的感受, so that 系统能记录我的主观体验。
17. As a 用户, I want to 选择故事发生时间, so that 系统能判断人生阶段。
18. As a 用户, I want to 在选择小时候或很久以前时补充年龄阶段, so that 时间标签更加准确。
19. As a 用户, I want to 主动选择国家和城市, so that 我能控制位置隐私。
20. As a 用户, I want to 多选故事中的人物关系, so that 故事上下文更完整。
21. As a 用户, I want to 开启专注模式, so that 写作时可以隐藏非必要内容。
22. As a 用户, I want to 每隔一段时间自动保存草稿, so that 刷新或离开不会丢失内容。
23. As a 用户, I want to 返回时恢复上次编辑位置, so that 我能连续完成首故事。
24. As a 用户, I want to 在大量粘贴时看到温和提示, so that 我能确认这是自己的既有内容。
25. As a 用户, I want to 在长时间停留时知道草稿已保存, so that 我可以安心休息。
26. As a 用户, I want to 提交故事给 AI 分析, so that 系统能整理标题、标签和叙事结构。
27. As a 用户, I want to 看到分阶段的分析进度, so that 等待过程具有意义而不是只有转圈。
28. As a 用户, I want to 在 AI 失败时重试, so that 临时故障不会阻断发布。
29. As a 用户, I want to 在连续失败时手动选择受控标签, so that 没有 AI 也能完成流程。
30. As a 用户, I want to 查看 AI 推荐标题, so that 我可以采用或修改它。
31. As a 用户, I want to 查看 Topic、Emotion、Meaning、Perspective 四层标签, so that 我能理解系统如何组织故事。
32. As a 用户, I want to 删除或替换不准确的标签, so that AI 不会替我定义经历。
33. As a 用户, I want to 预览正文与上下文, so that 发布前可以发现敏感信息或错误。
34. As a 用户, I want to 明确确认后再发布, so that 发布不是自动发生的。
35. As a 用户, I want to 在隐私风险提示后继续修改或仍然提交审核, so that 我保留对真实经历的控制。
36. As a 用户, I want to 发布成功后看到“故事成为星星”的反馈, so that 我知道操作已经完成。
37. As a 用户, I want to 发布后分别设置城市维度相似或相异, so that 地缘差异可以参与推荐。
38. As a 用户, I want to 分别设置人生阶段相似或相异, so that 我能遇见相近或不同生命阶段。
39. As a 用户, I want to 分别设置主题相似或相异, so that 我能控制内容熟悉度与新颖度。
40. As a 用户, I want to 理解三个维度的含义, so that 共鸣选择不会成为抽象算法配置。
41. As a 用户, I want to 获得目标 5 则推荐故事, so that 首次探索有清晰边界。
42. As a 用户, I want to 在候选不足时仍看到可用结果, so that 推荐不会因为数量不足而完全失败。
43. As a 用户, I want to 看到每则故事的推荐理由, so that 我知道相似或相异体现在哪里。
44. As a 用户, I want to 打开推荐故事并阅读完整正文, so that 我能理解另一个人的经历。
45. As a 用户, I want to 至少打开 1 则故事后进入轻量星图, so that 我不必被强制读完固定数量。
46. As a 用户, I want to 在轻量星图中看到所有公开故事, so that 我能进行开放探索。
47. As a 用户, I want to 通过星点大小感知故事长短, so that 星点视觉编码具有意义。
48. As a 用户, I want to 通过颜色与标签感知主题, so that 分类既直观又不只依赖颜色。
49. As a 用户, I want to 通过距离与推荐理由感知关系, so that 相似性不只是一个分数。
50. As a 用户, I want to 搜索情绪、城市、主题或人生转折, so that 我能快速定位故事。
51. As a 用户, I want to 按城市、人生阶段、主题和人物筛选, so that 我能聚焦特定故事集合。
52. As a 用户, I want to 切换星图和卡片视图, so that 我可以选择空间探索或高效阅读。
53. As a 用户, I want to 悬停或键盘聚焦星点查看预览, so that 我无需打开详情即可判断是否阅读。
54. As a 用户, I want to 点击星点在主页内打开详情层, so that 我不会离开探索上下文。
55. As a 用户, I want to 关闭详情后回到原来的星点或卡片位置, so that 探索状态不会丢失。
56. As a 用户, I want to 修改共鸣偏好, so that 推荐可以适应我当下的选择。
57. As a 用户, I want to 查看自己发布的故事, so that 我能管理个人叙事记录。
58. As a 用户, I want to 从主页继续创作故事, so that 表达和探索形成循环。
59. As a 用户, I want to 查看全部喜欢记录并按时间倒序排列, so that 我能重新访问产生共鸣的故事。
60. As a 用户, I want to 阅读标题、匿名作者、城市、阶段、标签和正文, so that 我能获得完整上下文。
61. As a 用户, I want to 在正文很长时于详情层内部滚动, so that 主页面位置保持不变。
62. As a 用户, I want to 看到 AIGC 图片的生成中、成功、失败和审核失败状态, so that 未来接入生图时页面结构稳定。
63. As a 用户, I want to 喜欢一个故事并可撤销, so that 我能表达并修正正向反馈。
64. As a 用户, I want to 不喜欢一个故事并可撤销, so that 我能表达并修正负向反馈。
65. As a 用户, I want to 喜欢和不喜欢保持互斥, so that 单个故事不会出现矛盾状态。
66. As a 用户, I want to 关闭故事而不反馈, so that 反馈不是强制的。
67. As a 用户, I want to 举报隐私泄露、仇恨骚扰、危险内容、垃圾内容或其他问题, so that 社区风险能够被处理。
68. As a 举报用户, I want to 填写可选文字说明, so that 审核人员能获得必要上下文。
69. As a 举报用户, I want to 在提交前二次确认, so that 我不会误报。
70. As a 举报用户, I want to 在成功后收到受理反馈, so that 我知道举报已经记录。
71. As a 用户, I want to 不看到公开评论区, so that 差异故事不会变成立场争论。
72. As a 研究人员, I want to 记录曝光、打开、共鸣选择和反馈事件, so that 我能评估产品闭环。
73. As a 隐私敏感用户, I want to 埋点中不包含邮箱明文、密码、故事正文和精确位置, so that 研究不会扩大隐私风险。
74. As a 隐私敏感用户, I want to 只记录写作时长、粘贴比例、修改次数和保存次数等聚合指标, so that 研究写作过程时不会保存逐键行为。
75. As a 管理员, I want to 查看故事、匿名作者、举报理由和说明, so that 我能理解案件上下文。
76. As a 管理员, I want to 保留、隐藏或删除内容, so that 不同风险得到适当处理。
77. As a 管理员, I want to 记录处理人、时间和理由, so that 审核行为可追溯。
78. As a 移动端用户, I want to 完成基础注册、输入和阅读, so that 核心能力在窄屏仍可使用。

## Implementation Decisions

### Experience Architecture

- 四个主体验模块：新用户引导／注册、Story Input Wizard、StoryVerse 主页、Story Resonance Output。
- Icebreaker 位于注册与正式故事之前，只提供浏览、预览和社区文化导入。
- 首个正式故事完成状态是服务端概念；localhost 用持久化模拟状态实现相同行为。
- 发布后进入三维共鸣选择，再进入首次推荐批次。
- 推荐批次目标为 5；打开至少 1 则后解锁轻量星图入口。
- Story Resonance Output 是主页内详情层，不是独立详情页。

### Visual Direction

- 主构图模型：OS-like editorial workspace。
- 标志性视觉机制：二维“故事轨道”——暖白画布上的星点、轨道线和可展开故事卡片。
- 使用暖白、黑色编辑感排版、电光蓝主操作、酸性青柠关键星点和少量红橙风险状态。
- PC 1440px 优先；保持大留白、细边框、低透明阴影和真实可用控件。
- 星图与卡片是同一数据的两种视图，不构建 3D、WASD 或复杂物理引擎。

### Localhost Architecture

- 第一阶段为单体前端原型，使用本地种子故事与浏览器持久化存储。
- 模拟身份、AI 分析、内容安全、推荐、图片状态和举报服务；接口形状与未来远端服务分离。
- 不接真实邮箱验证、数据库、模型 API、图片生成服务或文件上传。
- 中文为默认界面语言；文案集中管理，为后续国际化预留。

### Story Input and AI

- 正式故事流程为：人生事件引导、Story Collection、AI Analysis、Confirmation。
- 标题可选，正文建议 100–1500 字；低于建议值先给提示，不立即阻断。
- 支持输入和粘贴；语音转文字在 localhost 只保留入口与不可用说明。
- 草稿自动保存间隔为 15 秒，并在关键步骤立即保存。
- 聚合写作指标包括：开始时间、总时长、修改次数、粘贴字符比例、自动保存次数和回访次数。
- 禁止保存 Key Down、Key Up、Flight Time、Dwell Time、逐字符时间线或可复原正文的行为日志。
- AI 输出包括推荐标题、四层标签、Narrative Arc 和内部语义表示，但绝不返回改写正文。
- localhost 使用确定性的模拟分析结果和分阶段进度；连续失败后提供手动标签入口。

### Resonance, Feedback and Safety

- 共鸣维度只有城市、人生阶段、主题，每个维度分别存储 similar 或 different。
- “相异”结果仍需与用户故事存在可解释关联，不能随机抽取。
- 推荐理由说明一个主要相似点或差异点。
- Reaction 仅为 like 和 dislike，同一用户与故事组合唯一且互斥。
- 举报原因包括隐私泄露、仇恨或骚扰、危险内容、垃圾内容、其他；说明可选，提交前二次确认。
- localhost 不支持举报图片附件。
- AIGC 图片只实现生成中、成功、失败、审核失败的状态模型与占位 UI。

### Deep Modules

1. Session Gateway：封装注册状态、首故事门禁和恢复位置。
2. Draft Workspace：封装草稿、自动保存、字段校验和聚合写作指标。
3. Narrative Organizer：封装分析进度、结构化输出、失败与手动标签降级。
4. Story Lifecycle：封装草稿、分析、待确认、审核中、公开和拦截状态。
5. Resonance Engine：接收三个维度偏好，返回不超过 5 则候选及推荐理由。
6. Story Atlas Projection：把正文长度、主题和关系转换为稳定二维星点与卡片属性。
7. Reaction Ledger：封装喜欢／不喜欢互斥、撤销与喜欢记录。
8. Moderation Intake：封装举报原因、二次确认、受理状态与审核列表。
9. Event Contract：统一事件名、去重信息和隐私白名单。

## Testing Decisions

- 测试用户可观察行为和模块契约，不锁定组件树、CSS 类名或内部算法步骤。
- 模拟 AI 与推荐使用固定种子，断言结构、约束和状态变化，不逐字锁定生成文案。
- 关键流程覆盖成功、失败、刷新恢复、重复操作和无权限状态。
- 埋点测试同时断言应发送字段，以及密码、邮箱、正文和逐键数据不应出现。
- 必测模块：首故事门禁、草稿恢复、分析失败降级、故事状态机、三维推荐、轻量星图投影、反馈互斥、举报二次确认、详情关闭位置恢复和事件隐私契约。
- 端到端主路径：暖场浏览 → 注册 → 首故事 → AI 分析 → 发布 → 三维共鸣 → 5 则推荐 → 打开至少 1 则 → 进入轻量星图 → 喜欢／不喜欢／举报。

## Out of Scope

- 真实身份服务、邮箱验证、密码找回、数据库和多设备同步。
- 真实大模型、向量数据库、在线推荐模型与实时生成推荐理由。
- 真实 AIGC 生图、视频、四格分镜和自动分镜。
- 复杂 3D 星空、物理引擎、WASD 漫游和大规模图布局。
- 公开评论、收藏、关注、私信和社交关系链。
- 产品内显式异质性、理解度或满意度评分量表。
- 语音转文字、OCR、日记软件导入和举报图片附件。
- 逐键事件、击键生物特征、身份识别和可复原写作内容的行为日志。
- 完整管理后台与真实内容处置接口。
- GitHub 发布、线上部署和生产基础设施。

## Further Notes

- 本版本取代旧 PRD；旧版本保存在 `docs/prd-v1-archive.md` 供追溯。
- “3 个维度”与“四维共鸣选择”的冲突已统一为城市、人生阶段、主题。
- Icebreaker 明确为浏览功能，不计作正式故事，也不参与首故事门禁。
- 第一阶段完成 localhost 验证后，再评估真实服务接入、AIGC 单图与更丰富的主题细分。
