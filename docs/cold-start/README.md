# 冷启动数据目录

此目录用于在本地保存经过授权的冷启动故事源文件和导入文件。

- `*.xlsx`、`*.csv` 包含故事正文及导入元数据，仅保存在本地，不提交到公开 GitHub 仓库。
- 可提交的导入脚本位于 `scripts/`。
- 临时校验和转换产物统一写入项目根目录的 `outputs/`，该目录同样不会提交。
- 已导入的生产数据以 Supabase 数据库为准，代码仓库不作为故事内容备份。

发布前可使用相应脚本验证或导入，但不要移除 `.gitignore` 中针对该目录的保护规则。
